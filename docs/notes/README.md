# Notes

Context that outgrew a comment: decisions, incidents, invariants that
span files. One note per topic, referenced from the code it explains
(`// see docs/notes/<slug>.md`). Update the note in the same commit as
the code.

Plans live in `docs/plans/`, one per phase.
