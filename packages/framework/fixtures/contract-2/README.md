# contract-2 fixture

The contract-0 project plus what contract 2 adds: API handlers under
`routes/api/` in both shapes (named method exports and a default Hono
app). CONTRACT.md quotes from these files and a test proves the quotes
match. `scamp dev` runs here in the dev-server tests.

Not published as a standalone project; not type-checked by this repo's
`tsc`.
