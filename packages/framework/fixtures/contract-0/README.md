# contract-0 fixture

A tiny complete project in the shape CONTRACT.md specifies. CONTRACT.md
quotes from these files and a test proves the quotes match. Phase 1's
generator in the Scamp app is tested against `views/Lobby/Lobby.tsx`
byte for byte; phase 2 runs `scamp dev` here.

Not published, not type-checked by this repo's `tsc` (it depends on
`preact`, which the framework does not yet install).
