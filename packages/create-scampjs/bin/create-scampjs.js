#!/usr/bin/env node
// Contract 0 stub. The real scaffolder lands with the framework's
// templates export (phase 4). Until then this prints where to look and
// exits non-zero so nothing downstream mistakes it for success.
console.error(
  'create-scampjs is not ready yet. Follow https://github.com/scampdesign/scampjs for the first release.',
);
process.exit(1);
